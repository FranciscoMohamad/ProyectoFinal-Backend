<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles del Carrito</title>
</head>
<body>
    <h1>Detalles del Carrito</h1>

    {{#if cart.products.length}}
        <ul>
            {{#each cart.products}}
                <li>
                    <h3>Producto: {{this.product.title}}</h3>
                    <p>Cantidad: {{this.quantity}}</p>
                    <p>Precio: ${{this.product.price}}</p>
                    <p>Categoría: {{this.product.category}}</p>
                    <img src="{{this.product.thumbnails.[0]}}" alt="Imagen del producto">
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>El carrito está vacío.</p>
    {{/if}}

    <a href="/api/products"><button>Volver a la lista de productos</button></a>
</body>
</html>
